# /anon — placeholder

The `highli-anon` service (frustration submission + anonymous survey storage, separate process behind an identity-stripping proxy) lands in build #6. Plan: see [`docs/dx-redesign-plan.md`](../docs/dx-redesign-plan.md).
